# ─── Core LLM / Agent Framework ───────────────────────────────────────────────
groq>=0.9.0                 # Free LLM provider (llama-3.3-70b-versatile)
openai>=1.30.0              # Kept for SDK compatibility (not called if LLM_PROVIDER=groq)
langchain>=0.2.0
langchain-openai>=0.1.8
langchain-community>=0.2.0
langchain-core>=0.2.0
langgraph>=0.1.0
langsmith>=0.1.77

# ─── Embeddings (light — sentence-transformers instead of fastembed/BGE-M3) ───
# fastembed is too large for Streamlit Cloud free tier (600MB+ download)
sentence-transformers>=2.7.0

# ─── Vector Store ──────────────────────────────────────────────────────────────
pinecone>=3.2.2             # Version A (Pinecone) — enabled via RETRIEVAL_MODE=pinecone

# ─── Retrieval ─────────────────────────────────────────────────────────────────
rank-bm25>=0.2.2            # BM25 keyword retrieval

# ─── Data Ingestion ─────────────────────────────────────────────────────────────
httpx>=0.27.0
beautifulsoup4>=4.12.3
lxml>=5.2.1
pypdf>=4.2.0
tiktoken>=0.7.0             # Token counting for chunking

# ─── MCP ───────────────────────────────────────────────────────────────────────
fastmcp>=0.4.0

# ─── API ───────────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.1
sse-starlette>=1.8.2

# ─── UI ────────────────────────────────────────────────────────────────────────
streamlit>=1.35.0

# ─── Numerics ──────────────────────────────────────────────────────────────────
numpy>=1.26.4
scikit-learn>=1.4.2

# ─── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.1
tenacity>=8.3.0             # Retry logic for API calls
rich>=13.7.1
tqdm>=4.66.4
